package cs3500.imagecontroller;

/**
 * A class to represent the controller for the image processor system. Accepts input and directs
 * the model to perform actions based off of it.
 */
public interface ImageProcessorController {

  /**
   * A method that accepts input for the controller.
   */
  void acceptInput();
}

